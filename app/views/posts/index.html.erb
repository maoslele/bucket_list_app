<div class="main posts-index">
  <div class="container">
    <% @posts.each.with_index(1) do | post, i |%>
      <div class="posts-index-item">
        <%= link_to("#{i}. #{post.title}", "/posts/#{post.id}") %>
      </div>
    <% end %>
  </div>
</div>
