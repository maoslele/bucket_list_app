<div class="main posts-index">
  <div class="container">
    <% @posts.each do | post |%>
      <div class="posts-index-item">
        <%= link_to("#{post.id}. #{post.title}", "/posts/#{post.id}") %>
      </div>
    <% end %>
  </div>
</div>
